<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>nodejsbasic</title>
    <link href="/css/styles.css" rel="stylesheet"/>
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
</head>
<body>
   
   
    <div class="topnav">
        <a class="active" href="#home">Home</a>
        <a href="#news">News</a>
        <a href="#contact">Contact</a>
        <a href="#about">About</a>
    </div>
    <div>

        <div class="w3-container">
            <h2>List users Table</h2>
            <table class="w3-table-all w3-hoverable">
                <thead>
                    <tr class="w3-light-grey">
                        <th>ID</th>
                        <th>First Name</th>
                        <th>Last Name</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>

                    <% for(let i=0; i < dataUser.length; i++) { %>
                        <tr>
                            <td>
                                <%= dataUser[i].student_id %>
                            </td>
                            <td>
                                <%= dataUser[i].surname %>
                            </td>
                            <td>
                                <%= dataUser[i].given_names %>
                            </td>
                            <td>
                                <a href="/detail/user/<%= dataUser[i].student_id %>" target="_blank">Detail</a>
                                <form action="/edit/user/<%= dataUser[i].student_id %>" method="get">
                                    <button type="submit">Edit</button>
                                </form>
                                <form action="/delete/user" method="post" >
                                    <input type="text" name="userId" value="<%= dataUser[i].student_id %>" hidden/>
                                    <button type="submit">Delete</button>
                                </form>
                            </td>
                        </tr>
                        <% } %>
                </tbody>


            </table>
        </div>

        <div class="create-user-container">
            <form action="/create-new-user" method="POST">
                <div><b>Create a new user:</b></div>
                <div>
                    <label>Surname:</label>
                    <input type="text" name="surname" />
                </div>
                <div>
                    <label>givennames:</label>
                    <input type="text" name="givennames" />
                </div>
                <div>
                    <label>birthday:</label>
                    <input type="text" name="birthday" />
                </div>
                <div>
                    <label>year_encrolled:</label>
                    <input type="text" name="year" />
                </div>
                <div>
                    <button type="submit" class="btn-submit">Submit</button>
                </div>

            </form>

        </div>
    </div>
</body>
</html>